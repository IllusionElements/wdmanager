{
  "name": "wdmanager",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "@types/graphql": "^14.2.0",
    "lerna": "^3.14.1",
    "typescript": "^3.5.1"
  },
  "workspaces": [
    "packages/core",
    "packages/services"
  ],
  "scripts": {
    "build": "yarn tsc -p packages/core && yarn tsc -p packages/services",
    "dev": "yarn nodemon --inspect -r esm ./packages/core/dist"
  },
  "dependencies": {
    "dotenv": "^8.0.0",
    "esm": "^3.2.25",
    "nodemon": "^1.19.1"
  }
}
